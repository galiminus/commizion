- if @quotation.artist == current_user
  = t('.artist_page_title', { commissioner_name: @quotation.commissioner.name, title: @quotation.proposal.title })
- else
  = t('.commissioner_page_title', { artist_name: @quotation.artist.name, title: @quotation.proposal.title })
